import datetime
from model.model import User, db,Role
# db.create_all()
# #
# db.session.add(User(password='admin', username='admin', email='admin@fingertouch.com'))
# db.session.commit()

# user = User.query.filter_by(username='admin').first()
# print(datetime.datetime.now())
# user.last_login = datetime.datetime.now()
# db.session.commit()

# db.session.add(Role(type='admin'))
# db.session.commit()
